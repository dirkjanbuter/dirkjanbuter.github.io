function accessBonusStoryContent(id, mdName)
{
	var content = document.getElementById(id);
	var emailField = document.createElement("input");
    	emailField.type = "email";
	content.appendChild(emailField);
}
